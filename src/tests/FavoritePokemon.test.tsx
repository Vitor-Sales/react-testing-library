import { screen } from '@testing-library/react';
import renderWithRouter from '../renderWithRouter';
import App from '../App';

test('Testando página favorite pokémons: Se mostra a mensagem "No favorite pokémons found", Se ao favoritar pokémons mostram apenas os favoritados', () => {
  renderWithRouter;
});
